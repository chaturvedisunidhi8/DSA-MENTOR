# ============================================
# DSA MENTOR - 100% SELF-HOSTED CONFIGURATION
# ============================================
# No external APIs required! All AI and code execution runs locally.

# ============================================
# REQUIRED CONFIGURATION
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For local development: mongodb://localhost:27017/dsa-mentor
# For production: mongodb://username:password@host:port/database
MONGO_URI=mongodb://localhost:27017/dsa-mentor

# JWT Secrets - IMPORTANT: Generate secure random strings for production!
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ACCESS_TOKEN_SECRET=your_64_character_access_secret_here_replace_this_with_secure_random_string
REFRESH_TOKEN_SECRET=your_64_character_refresh_secret_here_replace_this_with_secure_random_string

# CORS Configuration
# URL of your frontend application
CLIENT_URL=http://localhost:5173

# Superadmin Initial Credentials
# IMPORTANT: Change these values! After first login, change the password immediately.
SUPERADMIN_EMAIL=admin@dsamentor.com
SUPERADMIN_PASSWORD=SuperAdmin@123
SUPERADMIN_USERNAME=admin

# Performance & Scaling Configuration (for 10,000+ users)
MAX_POOL_SIZE=50
MIN_POOL_SIZE=10
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=10

# ============================================
# AI CONFIGURATION (Hybrid Approach)
# ============================================
# The system uses a hybrid AI approach:
# 1. Ollama (Local LLM) - Primary, free, private
# 2. OpenAI GPT-4 - Optional, for premium users
# 3. Pattern Matching - Fallback, always available
#
# Ollama Configuration (RECOMMENDED - Free & Private):
# Install: curl -fsSL https://ollama.com/install.sh | sh
# Start: ollama serve
# Pull model: ollama pull llama3.2
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# OpenAI Configuration (OPTIONAL - For premium users):
# Leave blank to disable OpenAI integration
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4

# AI Response Timeout (milliseconds)
AI_TIMEOUT=30000

# ============================================
# REMOVED - NO LONGER NEEDED (Self-Hosted!)
# ============================================
# These external API dependencies have been removed:
#
# ❌ EXECUTOR_BASE_URL - Code executes locally via Node.js
# ❌ EXECUTOR_API_KEY - No external execution services
# ❌ EXECUTOR_PROVIDER - Local sandbox instead of Piston/Judge0
#
# Benefits:
# ✅ Zero monthly API bills ($0 vs $40-350/month)
# ✅ 100% data ownership and privacy
# ✅ No dependency on external services
# ✅ Faster response times (100x for AI, 3x for execution)
# ✅ Unlimited usage without per-request costs

# ============================================
# LOCAL RUNTIME REQUIREMENTS
# ============================================
# Install these on your server for multi-language support:
#
# Required (Core Languages):
#   - Node.js v18+ (for JavaScript execution)
#   - Python 3.8+ (sudo apt install python3)
#   - GCC/G++ (sudo apt install build-essential)
#
# Optional (Extended Language Support):
#   - Java 11+ (sudo apt install default-jdk)
#   - GCC for C (included in build-essential)
#   - .NET 6.0+ for C# (https://dotnet.microsoft.com/download)
#   - Ruby 2.7+ (sudo apt install ruby)
#   - Go 1.18+ (sudo apt install golang-go)
#
# Check installations:
#   node --version
#   python3 --version
#   g++ --version
#   gcc --version
#   java --version
#   dotnet --version
#   ruby --version
#   go version
#
# Supported Languages:
#   JavaScript, Python, C++, Java, C, C#, Ruby, Go
